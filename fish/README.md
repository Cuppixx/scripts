# README

Fish scripts (.fish) are command-line scripts that run in the Fish shell, a modern alternative to bash with different syntax.
You can run shell scripts (.sh) from Fish by using a shebang like `#!/bin/bash` at the top of the shell-script.
